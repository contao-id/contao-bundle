{% set path = '/bundles/contaoidcontao/' ~ (isLegacy ? 'legacy/' : '') ~ (isAdmin ? 'admin' : 'user') ~ '.svg' %}

<div title="Provided by contao.id" class="list_icon_new" style="background-image:url({{ path }})"  data-icon="{{ path }}">
    &nbsp;
</div>
